// this is an exapmle of authentication.


// importing Tasks/ Action Handlers
// import { authenticate, signUp } from './auth';
// importing ActionTypes
// import { ActionTypes as planActionTypes } from '../contexts/usePlanContext'

/**
 * Middleware (interceptador de actions)
 */
export default function dispatchEffect (action, dispatchers) {

    // switch ( action.type ) {

    //     case planActionTypes.SET_PLAN :
    //         return authenticate(action, dispatchers);

    //     default:
    //         break;
    // }

}